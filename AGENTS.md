# xianyu-agent 编码规则

- 使用 Python；代码注释、终端提示和面向用户的文案统一使用中文。
- 商品价格、版本、交付、售后、配置等事实必须来自本地资料或明确数据源，禁止由模型编造。
- Agent 工作流必须设置循环上限和失败时的安全兜底。
- 不在代码、测试、日志或文档中写入 API Key、密钥或其他私密配置。
- 新增运行时依赖时，同步更新 `requirements.txt`。
- 编写或修改业务代码时，可以临时编写和运行测试用于验证；交付前不要保留新建的回归测试文件或用例，除非用户明确要求保留测试。
- 除非用户明确要求，不接入真实闲鱼订单、发货、退款或支付操作。
