# Agent 框架：后续用来组合模型、消息和工具。
langchain==1.4.0
# 显式声明多 Agent 状态机依赖，避免仅依赖 LangChain 的传递安装。
langgraph>=1.0.0,<2.0.0
# DeepSeek 模型适配器。
langchain-deepseek==1.1.0
# LangChain / LangGraph 调用链追踪；未配置 LANGFUSE_* 密钥时不会启用。
langfuse==4.15.4
# 将本地 .env 文件读取为环境变量。
python-dotenv==1.2.3
# 本地 BM25 检索使用的中文分词器。
jieba==0.42.1
# 中文语义向量，本地 CPU 推理。
fastembed==0.8.0
# 对外提供 Plus 调用的本地客服 HTTP 接口。
fastapi==0.115.12
uvicorn[standard]==0.34.2
